# Mobility Recovery Guide

Netlify settings:

- Build command: `npm run build`
- Publish directory: `dist`

This version pins Tailwind CSS to v3 so the included PostCSS config works reliably on Netlify.
